import styled from '@emotion/styled';
import { fadeInScale } from '~/styles/keyframes';

const TransitionLayout = styled.div({
  animation: `0.15s ${fadeInScale} both`,
});

export default TransitionLayout;
